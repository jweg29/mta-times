import { Stop } from "../lib/definitions";

type StopSelectionProps = {
    selectedStop: Stop | null;
    onSelectStop: (stop: Stop | null) => void;
};

export default async function DepartureTimes() {
    return <p>...</p>
}
